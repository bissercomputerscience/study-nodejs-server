const express = require('express');
const app = express();
const port=8080;

app.get('/', handleIndex)

app.listen(port, ()=>{
    console.log("Server started in dev mode")}
)

function handleIndex(req, res) {
    return res.send('Test GitHub Action - test 03')
}